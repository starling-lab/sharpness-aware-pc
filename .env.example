# Core paths
DATA_ROOT=data
RESULTS_DIR=results

# Logging (override per user)
WANDB_PROJECT=pc-hessian
WANDB_ENTITY=
WANDB_MODE=offline   # set to "online" to sync
WANDB_API_KEY=

# Compute
CUDA_VISIBLE_DEVICES=0
TORCH_NUM_THREADS=8

# Optional: custom .env path can be set via HESSIAN_ENV_FILE
